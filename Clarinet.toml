[project]
name = 'Decentralized-Digital-Identity-Interoperability-Hub'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.identity-provider-registry]
path = 'contracts/identity-provider-registry.clar'
clarity_version = 2
epoch = 2.5

[contracts.identity-provider-registry_]
path = 'contracts/identity-provider-registry_.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
